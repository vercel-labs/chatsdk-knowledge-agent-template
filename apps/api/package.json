{
  "name": "@savoir/api",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev --port 3001",
    "preview": "npx srvx --prod .output/",
    "sync:docs": "curl -X POST http://localhost:3001/api/sync",
    "snapshot:create": "curl -X POST http://localhost:3001/api/sandbox/snapshot",
    "workflow:web": "npx workflow web",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "@savoir/config": "workspace:*",
    "@savoir/logger": "workspace:*",
    "@types/node": "^25.0.9",
    "@upstash/redis": "^1.36.1",
    "@vercel/sandbox": "^1.2.0",
    "giget": "^2.0.0",
    "pathe": "^2.0.3",
    "workflow": "^4.0.1-beta.48",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "eslint": "^9.28.0",
    "nitro": "latest",
    "rolldown": "latest",
    "typescript": "^5.8.3"
  }
}
