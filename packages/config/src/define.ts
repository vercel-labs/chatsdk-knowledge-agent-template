import type { SavoirConfigInput } from './types'

/**
 * Define the Savoir configuration.
 * Provides type safety and autocompletion in savoir.config.ts
 */
export function defineConfig(config: SavoirConfigInput): SavoirConfigInput {
  return config
}
